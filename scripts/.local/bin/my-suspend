#!/usr/bin/env janet

(defn fish [s]
  ```
  Run command as fish shell
  ```
  (os/execute @("/usr/bin/fish" "-c" s) :e))

# TODO: sy pull
(defn main [& args]
  ```
  Sync my stuff
  ```
  (fish "sy push")
  (os/shell "i3lock -c 263238 && systemctl suspend"))
